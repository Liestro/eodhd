services:
  eodhd:
    build: .
    container_name: eodhd
    command: python main.py
    depends_on:
      - mongodb

  mongodb:
    image: mongo
    ports:
      - '27017:27017'
    volumes:
      - mongodbvol:/data/db

volumes:
  mongodbvol: